describe('testing the guide buttons', () => {
  it('is clicking the button "Weiter" to navigate to the next page', () => {
    cy.visit('/task/1')
    cy.contains('Weiter').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/2')
    cy.contains('Weiter').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/3')
    cy.contains('Weiter').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/4')
    cy.contains('Weiter').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/5')
    cy.contains('Weiter').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/6')
    cy.contains('Weiter').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/7')
    cy.contains('Weiter').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/8')
    cy.contains('Weiter').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/9')
    cy.contains('Weiter').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/10')
    cy.contains('Weiter').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/11')
    cy.contains('Weiter').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/12')
    cy.contains('Weiter').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/13')
    cy.contains('Weiter').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/14')
    cy.contains('Weiter').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/15')
    cy.contains('Weiter').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/16')
    cy.contains('Zurück').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/15')
    cy.contains('Zurück').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/14')
    cy.contains('Zurück').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/13')
    cy.contains('Zurück').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/12')
    cy.contains('Zurück').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/11')
    cy.contains('Zurück').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/10')
    cy.contains('Zurück').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/9')
    cy.contains('Zurück').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/8')
    cy.contains('Zurück').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/7')
    cy.contains('Zurück').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/6')
    cy.contains('Zurück').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/5')
    cy.contains('Zurück').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/4')
    cy.contains('Zurück').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/3')
    cy.contains('Zurück').click()
  })

  it('is clicking the button to navigate within the guide', () => {
    cy.visit('/task/2')
    cy.contains('Zurück').click()
  })
})
